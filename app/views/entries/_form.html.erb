<%= form_with(model: entry, local: true, :builder => Judge::FormBuilder) do |form| %>
  <% if entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% entry.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :entry_title %>
  </div>

  <div class="field">
    <%= form.label :subhead %>
    <%= form.text_area :subhead, id: :entry_subhead, maxlength: 255 %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :entry_body %>
  </div>

    <%= form.hidden_field :author, value: @current_admin %>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
